# Lockwood IT Solutions — Regenerated

Next.js App Router + Tailwind + Builder.io + Portal + MobileNav.

## Quickstart
```bash
npm i
cp .env.example .env.local   # add your Builder PUBLIC API key
npm run build
npx serve ./out
```
